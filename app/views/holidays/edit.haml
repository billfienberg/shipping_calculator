%h3 Add a New Holiday
= form_for @holiday do |h|
  = h.label :name
  = h.text_field :name, placeholder:"New Years Eve"
  = h.label :date
  = h.text_field :date, placeholder:"yyyy-mm-dd"
  = h.submit
= link_to "View All Holidays", holidays_path